import blog from "./blog-schema";
import brand from "./brand-schema";
import category from "./category-schema";
import product from "./product-schema";

const schemas = [product, category, brand, blog];

export default schemas;
